name: hangman
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  http: ^1.2.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cop_adam.png   #  dosyayı tek tek ilan etmek zorunda kaldım
    - .env
